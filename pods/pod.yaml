# kubectl create -f pod.yaml      para criar o pode deste arquivo
# kubectl delete pod nome_do_pod
apiVersion: v1
kind: Pod
metadata:
  name: nginx-2
  labels:
    app: nginx-2
    tier: frontend
spec:
  containers:
    - name: nginx-2
      image: nginx
